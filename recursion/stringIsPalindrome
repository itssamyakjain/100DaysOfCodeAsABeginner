// ispalindrome by recursive method
static boolean isPalindrome(String str, int s,int e) {
		
		if(s==e)
			return true;  //base case if 
		
		if(s<e)
			return true;
		
		if(str.charAt(s)!=str.charAt(e))
			return false;
		
		else
			return isPalindrome(str,s+1,e-1);			
}
