// method returns indices of two numbers that adds to the target
public int[] twoSum(int[] nums, int target) {
        int[] targetIndices = new int[2];
        
        for(int i=0;i<nums.length;i++){
                if(nums[i]+nums[j]==target){
                    targetIndices[0]=i;
                    targetIndices[1]=j;
                }
                    
        }
        return targetIndices;
    }
